<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.community.dao.ZyCommunityInteractionDao">

    <resultMap type="com.example.community.entity.ZyCommunityInteraction" id="ZyCommunityInteractionMap">
        <result property="interactionId" column="interaction_id" jdbcType="INTEGER"/>
        <result property="communityId" column="community_id" jdbcType="INTEGER"/>
        <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
        <result property="updateBy" column="update_by" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="delFlag" column="del_flag" jdbcType="INTEGER"/>
        <result property="remark" column="remark" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="interactionId" useGeneratedKeys="true">
        insert into zy-community.zy_community_interaction(community_id, create_by, update_by, create_time, update_time, content, del_flag, remark, user_id)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.communityId}, #{entity.createBy}, #{entity.updateBy}, #{entity.createTime}, #{entity.updateTime}, #{entity.content}, #{entity.delFlag}, #{entity.remark}, #{entity.userId})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="interactionId" useGeneratedKeys="true">
        insert into zy-community.zy_community_interaction(community_id, create_by, update_by, create_time, update_time, content, del_flag, remark, user_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.communityId}, #{entity.createBy}, #{entity.updateBy}, #{entity.createTime}, #{entity.updateTime}, #{entity.content}, #{entity.delFlag}, #{entity.remark}, #{entity.userId})
        </foreach>
        on duplicate key update
         community_id = values(community_id) , create_by = values(create_by) , update_by = values(update_by) , create_time = values(create_time) , update_time = values(update_time) , content = values(content) , del_flag = values(del_flag) , remark = values(remark) , user_id = values(user_id)     </insert>

</mapper>

